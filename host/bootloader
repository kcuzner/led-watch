#!/usr/bin/env python3

from device import bootloader

import sys

def main():
    dev = bootloader.find_device()
    if dev is None:
        sys.exit('No device found')
    with dev:
        print(dev.bootloader_command(bootloader.ResetCommand()))

if __name__ == '__main__':
    main()

